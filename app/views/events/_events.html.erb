<div id="events">
	<div class="event new_event">
		<div class="clearfix"><h2>Add new event</h2></div>
	<% form_tag(plan_events_url(plan)) do  %>
	 <%= render :partial => "events/form" %>
	<% end %>
	</div>
	
	<%= render :partial => events %>
</div>